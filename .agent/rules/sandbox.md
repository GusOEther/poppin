# Workspace Rules: Sandbox Security

- All project files are located in WSL (Ubuntu) at /home/mark/projects/poppin.
- The AI agent MUST NOT access or modify files on the Windows host system (C:\, etc.).
- All terminal commands MUST be executed within the Ubuntu WSL environment.
- Use the network path \wsl.localhost\Ubuntu\home\mark\projects\poppin for file operations.
